# release 签名配置模板
# ====================================================================
# 把这个文件复制成 apps/mobile/keystore.properties 再填真实值。
# 🔴 keystore.properties 和 .jks 文件**都在 .gitignore 里，永远不要提交**。
#    这个仓库是公开的，密码进了 git 历史就等于公开了 ——
#    别人拿它可以签一个假的 Synorive，覆盖安装到你的用户手机上。
#
# 没有这个文件时 `assembleRelease` 不会报错，只会打出一个**没有正式签名**的 APK
# 并在构建日志里打一行警告。所以：出正式包之前，先确认这个文件在。
#
# ── 怎么生成密钥库 ──────────────────────────────────────────
# JDK 自带 keytool（Android Studio 里那个 JDK 也有）：
#
#   keytool -genkeypair -v \
#     -keystore D:/keys/synorive-release.jks \
#     -alias synorive -keyalg RSA -keysize 4096 -validity 10000 \
#     -storepass <你的口令> -keypass <你的口令> \
#     -dname "CN=Synorive, OU=Synorive, O=Synorive, L=, S=, C=CN"
#
# 或者直接跑 `node scripts/make-android-keystore.mjs`（会问你要口令）。
#
# 🔴 **密钥库丢了 = 这个应用再也无法发布能覆盖安装的更新。**
#    Android 靠签名判断"是不是同一个应用"，换了密钥的包在用户手机上
#    只能先卸载再装（数据全丢）。备份到你自己的地方，不要只留一份。
#
# storeFile 是**相对 apps/mobile/ 的路径**，也可以写绝对路径。
# 推荐放到仓库外，绝对路径最保险。

storeFile=D:/keys/synorive-release.jks
storePassword=在这里填密钥库口令
keyAlias=synorive
keyPassword=在这里填密钥口令
