# Synorive 功能增强与验收指标方案库

> 本文件是本轮方案阶段交付物。它把“功能增强、性能、算法、界面、安全、跨端和 MCP”拆成可选择的候选项；当前不执行应用代码改动、GitHub 上传、Release 删除或本地旧版本删除。
>
> 状态口径：`已有代码/未验证` 表示在当前工作区找到实现线索，但没有把它当作用户可用证据；`局部能力/未闭环` 表示存在一部分路径；`候选新增` 表示尚未实施。本文件的建议值是验收目标，不是当前实测结果。

## 先做的选择

建议先选一条产品主线，再从菜单中增删项目：

| 方向 | 你会得到什么 | 代价 | 出问题时的样子 |
|---|---|---|---|
| **P-A 证据优先工作台（推荐）** | 搜索、研究、引用、反证、数字校对和 MCP 都围绕“可回查”统一；优先修复静默空结果和证据断链 | 首屏信息模型会重排，需把现有结果卡片、研究页和 MCP 输出接到同一套 provenance 数据 | 功能看似都在，但某个结果无法回到原文时，该结果被标成未验证并保留原始错误，不伪装成结论 |
| **P-B 极速检索舱** | 先把首屏结果、批量投喂、缓存、调度和 UI 帧率做到稳定；研究深挖与安卓增强后置 | 多模态深度描述和证据图谱会暂缓 | 复杂分析不会阻塞检索；如果超过预算，系统会降级并显示阶段耗时，不会空白等待 |
| **P-C 跨端随身库** | 电脑负责索引，手机/平板负责搜索、拍照即搜、分享即存、离线最近结果和更新 | 局域网认证、安卓更新和离线冲突会成为第一优先级 | 电脑离线时手机只能显示缓存，并明确“缓存时间/来源”，不会把旧结果冒充实时结果 |

推荐主线是 **P-A**：Synorive 已经存在检索、研究和 MCP 的多条能力，真正的差异化不是再加一个入口，而是让每条重要结果都能说明“来自哪里、经过什么处理、还缺什么证据”。P-B 适合当前最关心秒开和低卡顿的情况；P-C 适合手机/平板是主要使用入口的情况。

### GUI 方向

| 方向 | 视觉与布局 | 适用场景 | 失败表现与修正 |
|---|---|---|---|
| **G-A 学术纸张工作台（推荐）** | 暖白/深暖灰、墨蓝主色、证据链用朱砂色；左侧模块树，中间大主图/结果，右侧按需展开证据 | 长时间阅读、研究、引用、PDF 和文献对照 | 信息密度过低会浪费大屏；用结果密度切换和大图沉浸模式修正 |
| **G-B 深色证据图谱** | 深色背景、低饱和语义色、中央关系图和时间轴；冲突与缺证据用稳定颜色编码 | 反证、关系追踪、时间线、跨来源比较 | 节点太多会变成“蜘蛛网”；限制默认显示层级，点击后逐层展开 |
| **G-C 分屏控制舱** | 左侧输入/队列，中间结果舞台，右侧性能与安全状态；底部固定可回查条 | 批量投喂、长期监控、需要随时看资源占用的用户 | 状态面板抢走注意力；默认折叠诊断，仅在异常或用户打开时占位 |

默认推荐 **G-A**，但不会把它写死为唯一方案；三份可双击打开的预览在 `docs/方案预览-学术工作台.html`、`docs/方案预览-证据图谱.html`、`docs/方案预览-分屏控制舱.html`，主选择板在 `docs/方案选择板.html`。

## 选择规则

- `R` 是本轮推荐，`K` 是当前已有代码但必须重新验证，`N` 是候选新增，`G` 是 GUI/证据交付项。
- P0 闭环项不能被“更快”平均掉：原有有效行为保护、用户端复现、真实浏览器验证、隐私扫描、更新链路和回滚证据属于硬门槛。
- 选择某项不等于它已经实现。实现以后必须有对应的用户路径、原始输出、屏幕/文件证据或可重复的基准结果。
- 方案中涉及写入、联网、安装、上传、发布、删除或签名的动作，必须在执行阶段重新核对具体目标、版本、哈希、隐私扫描和回滚点。

## 方案菜单

### A. 检索与结果质量

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| A01 | R/K | 分阶段首屏检索 | 前缀/最近结果先出，FTS5 关键词召回后追加，向量和精排异步补齐；结果不清屏 | 本地库搜索 | 首次可见反馈 P95 ≤100ms；最终排序 P95 ≤500ms；结果列表不跳回顶部 | 后续阶段失败时保留已显示结果并标明缺失阶段；当前已有类似路径，端到端未验证 |
| A02 | R/K | 查询感知的混合召回 | 关键词、向量、拼音、字段权重按查询类型选择；用 RRF 融合排名，避免直接相加不同量纲分数 | 中英文、代码、中文短问句 | 100 条人工标注查询 Top5 命中率相对基线提升 ≥20%，同时记录零结果率 | 语义模型不可用时退回关键词并显示降级原因；已有混合检索，指标未验证 |
| A03 | N | 结构化查询预览 | 把 `type/date/in/tag/section` 解析为可点击条件，显示“实际搜索词”和“实际过滤条件” | 高级搜索、中文用户 | 100% 的合法样例都能显示解析结果；非法条件不得静默改变查询 | 解析失败退回普通关键词并提示；不能把用户输入当 SQL 直接拼接；候选新增 |
| A04 | R/K | 词汇桥与拼音兜底 | 同义词、简称、中文/英文、拼音首字母分别生成受限扩展，记录扩展前后词 | 人名、项目名、代码名 | 召回率提升 ≥15%，误召回率增加 ≤10%；每条结果可查看命中词 | 扩展过宽会把无关结果推上来；支持关闭和回到原查询；局部能力未闭环 |
| A05 | N | 跨模态查询路由 | 先判断输入是文字、图片、视频帧、链接或文件，再选择相应召回通道；不让不兼容的通道返回空壳 | 图片找文、文字找视频、链接归档 | 5 类输入均有明确路由日志；不支持的输入在 1 秒内给出可操作原因 | 路由不确定时走关键词/文件元数据最低能力，不伪造语义匹配；候选新增 |
| A06 | R/K | 视频命中时间码 | 语音片段、场景切分和关键帧共享时间轴；点击结果直接跳到秒级位置 | 视频/音频 | 命中时间误差 ≤2 秒；无音轨视频仍能用场景/关键帧搜索 | 没有可用时间信号时标出“等分时间段”，不声称是内容边界；局部能力未闭环 |
| A07 | R/K | 近重复聚类与可逆清理 | 内容指纹、pHash、文本归一化和大小/时间元数据联合聚类；删除先入回收站 | 大量照片、备份和重复文档 | 1000 个构造重复样本聚类准确率 ≥95%；清理后原文件不被删除 | 视觉哈希对局部小改动不敏感；必须保留“只清索引/删除原文件”的边界；已有检测，UI 未闭环 |
| A08 | N | 证据上下文包 | 结果点击时同时取命中块、相邻块、页码/时间码、来源版本和指纹，形成可复制的证据包 | 研究、MCP、导出 | 每个引用都有稳定 item/chunk/locator；缺上下文时明确缺失字段 | 文件被移动或网页失效时保留快照与原定位；候选新增 |
| A09 | R/K | 可解释排序面板 | 展示关键词名次、语义名次、字段命中、时间新鲜度和筛选原因；拒绝只显示一个神秘分数 | 专业用户排查“为什么是它” | 任意 Top20 结果均能列出至少一个实际命中理由；排除结果可回看 | 解释字段缺失时显示“无该证据”，不编造理由；已有组件，未做真实对拍 |
| A10 | G/N | 检索黄金集 | 建立 20–100 条真实查询及期望文档/时间码/反例，作为回归基线；它是产品评估集，不替代用户端验收 | 每次调整排序/分词/模型 | 同一数据集 Top5、零结果率、时间码误差和 P95 延迟可重复 | 集合偏向某一类资料会误导结论；必须标出样本范围和未覆盖模态；候选新增 |

### B. 摄取、模型与性能

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| B01 | R/K | 分阶段队列 | fingerprint、parse、enrich、chunk、embed、write 分阶段计时与队列；可按阶段暂停/重试 | 大批量导入 | 每阶段有 completed/failed/skipped 和耗时；强杀后已完成阶段不重做 | 只有总进度没有阶段状态时会掩盖瓶颈；已有断点路径，未完成全量复验 |
| B02 | R/K | 前台优先调度 | 用户当前打开/刚查询的文件优先，后台分析低优先级；限制 worker 数量和队列占用 | 批量分析时继续搜索 | 满载时前台操作 p99 帧时间增幅 ≤20%；搜索 P95 不超过空载基线的 1.5 倍 | 并发过高会争抢 CPU；至少保留“暂停后台任务”硬开关；局部能力未闭环 |
| B03 | N | 自适应并发控制 | 根据 CPU、内存、I/O 等待和搜索延迟动态调节并行度，并设上下限与冷却时间 | 不同电脑配置 | 不超过用户上限；队列运行 10 分钟无振荡；搜索延迟回到预算内 | 传感器抖动可能频繁调度；异常时降到保守并发并留下原因；候选新增 |
| B04 | R/K | 模型能力医生 | 启动时探测模型文件、依赖、执行器和版本；每项给出“可用/降级/缺失/未验证” | 全部本地模型 | 不因模型缺失导致主界面白屏；依赖状态与实际首次调用结果一致 | 只检查文件存在会造成假绿；必须执行最小真实推理探针；已有医生，运行态未验证 |
| B05 | N | 模型按需加载与淘汰 | 文字、OCR、视觉、ASR 模型按请求加载，空闲后释放；正在使用的模型锁定 | 低内存电脑、混合模态 | 冷启动不加载未用模型；连续三种模态后 RSS 不持续单向增长 | 频繁切换模型会增加首请求延迟；显示冷启动/热命中分开耗时；候选新增 |
| B06 | N | 可验证模型缓存 | 模型下载写临时文件，校验长度/摘要后原子替换；支持断点和坏缓存隔离 | 模型安装、更新 | 中断后可续传；摘要不匹配不会加载；坏包有可恢复清理路径 | 只比文件大小不能证明内容正确；需保存来源、版本和摘要；候选新增 |
| B07 | R/K | 文件变化增量索引 | fingerprint 和 mtime/size 只触发真正变化的阶段；删除/移动通过稳定 ID 更新引用 | watched folder、重复导入 | 同一文件二次扫描不重复建块；移动后搜索结果路径正确 | 网络盘、时钟回拨和权限变化会破坏假设；异常进入待人工处理队列；局部能力未闭环 |
| B08 | R/K | 数据库写入隔离 | WAL、事务和批量写；分析进程不直接改 UI 状态，统一通过事件/查询读取 | 长时间导入、多窗口 | 强杀后数据库可打开；已提交记录可搜；未提交批次可重做 | 断电模拟需要实际设备条件；当前只能标为未验证，不能写“永不损坏” |
| B09 | N | 作业事件日志 | 每个 job 有状态机、阶段、重试次数、错误原文和恢复动作；历史状态可重建 | 后台分析、更新、同步 | 任意失败任务都能回答“卡在哪一步、为什么、下一步” | 事件丢失会造成假进度；写入必须幂等并限制日志大小；候选新增 |
| B10 | G/N | 性能预算面板 | 区分冷启动、热启动、首结果、最终结果、UI 帧、引擎 RSS、磁盘和队列等待 | 性能优化和用户诊断 | 指标来源、样本量、时间范围和目标同屏可查；无样本显示“未采样” | 运行期样本不能替代基准；当前已有 `metrics.py`，部分目标仍未测 |

### C. 研究、证据与知识回查

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| C01 | R/K | 研究预算与质量联动 | 超时不只结束请求，还记录跳过了哪一轮、少了哪些来源和证据；P95 与降级率一起看 | 深挖研究 | 同时报告 P95、来源数、第二轮完成率和降级率 | 只优化延迟可能得到缩水简报；已有 deadline 路径，需复验质量代价 |
| C02 | R/K | 来源证据分层 | 区分官方/原始/二手/转载/未知；同文转载合并但保留来源列表 | 联网搜索、文献 | 每个事实有来源类型和时间；转载不会被算成多源独立支持 | 来源身份不明时只能写未知；不能用颜色替代证据说明；局部能力未闭环 |
| C03 | R/K | 反证并排 | 支持、反驳、限制和未决材料并排显示，不把“存在反驳”改写成“原命题为假” | 核查、研究 | 结论至少有一个支持/限制字段；冲突来源不被静默合并 | 证据冲突不能自动裁决；显示双方原文和日期；已有能力，未全链路验证 |
| C04 | R/K | 数字回原文 | 提取简报中的数字、单位、时间窗，逐个回到来源文本核对；区分未取到原文和不匹配 | 研究简报、导出 | 每个数字标记 ok/mismatch/unverified，带原文上下文 | mismatch 不等于事实错误；必须显示最接近候选和单位差异；已有能力，需实测 |
| C05 | N | 证据图谱双向导航 | 结论→节点→箭头→证据→原文，原文→被引用结论；返回保留筛选和动画状态 | 复杂解释、HTML、MCP | 每个重要结论/节点/关系都有稳定 ID；返回后状态不重置 | 外部网页可能无稳定段落锚点；必须明确技术限制；候选新增 |
| C06 | N | 版本化快照 | 网页、文件、论文版本和抓取时间进入证据身份；同一 URL 不覆盖历史版本 | 网页归档、长期课题 | 原文变化后旧结论仍可回查旧快照；快照带 hash/时间 | 存储增长和版权边界需控制；只存用户允许的内容；候选新增 |
| C07 | N | 研究项目状态机 | draft/running/paused/needs-review/complete 分离；每个阶段可恢复而不是重新深挖 | 长期研究 | 关闭应用重开后项目、来源、失败阶段和下一动作仍在 | 状态写成功但数据未落盘会产生假完成；需事务和重启复验；候选新增 |
| C08 | G/N | 解释型 HTML 回查器 | 大图展示模块与关系，点击节点显示证据、来源和反向陈述；动画支持暂停、单步和速度 | 技术原理、产品方案、研究报告 | 关键箭头可生成“节点 A 在条件 C 下影响 B”的可核对陈述；每条带证据状态 | 视觉关系若无语义会误导；所有箭头必须标注类型；候选新增 |

### D. Claude Code、MCP 与自动化边界

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| D01 | R/K | MCP 工具分层 | `search/status/get_content` 为只读高频层，`ingest/analyze/export` 为显式动作层，描述写清边界 | Claude Code | 新会话可发现工具；每个工具 schema 可校验；错误返回可操作 | 工具数量多会增加选择噪音；保留稳定命名和兼容别名；已有 26 工具，未做新会话全量验证 |
| D02 | N | MCP 能力协商 | 暴露版本、支持模态、证据级别、延迟预算和隐私限制，让模型先知道能做什么 | 多模型、未来客户端 | Claude Code 能通过一个只读状态调用发现能力；不支持能力不会生成空调用 | 能力声明过时会误导模型；启动探针必须与实际工具路径一致；候选新增 |
| D03 | R/K | MCP 输出证据合同 | 每个结果带 source、locator、trust、limits、unverified 字段；不把空列表当成功 | Claude Code 答案 | 空结果、降级和冲突均有明确语义；输出中无未标记的生成性总结 | 客户端可能忽略字段；工具描述和返回结构双重写限制；局部能力未闭环 |
| D04 | N | CLI JSON 稳定模式 | `--json` 输出版本化 schema，机器消费与人类彩色输出完全分开 | 脚本、其他 AI | schema version 稳定；错误走 stderr 和非零退出；stdout 不混日志 | 终端颜色或日志混入会破坏流水线；需黄金输入输出集；候选新增 |
| D05 | N | 本地能力注册表 | Claude Code、CLI、桌面端共读能力和版本注册表，避免三套功能清单漂移 | 多入口一致性 | 26 工具/CLI/界面显示的能力来源可追到同一 ID | 注册表滞后会产生“文档有而工具没有”；构建时生成并运行探针；候选新增 |
| D06 | N | 自动化默认只读 | 自动扫描、更新检查和状态同步默认只读；写入、联网、导出、安装和删除需要具体动作 | 用户信任、后台常驻 | 所有高影响操作都有动作日志、目标和可撤回/回滚说明 | 过度确认会降低可用性；同一会话已授权的可逆动作可连续执行；候选新增 |

### E. 桌面 GUI、托盘与可用性

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| E01 | R/K | 托盘有限状态机 | 单击显示/隐藏，菜单“打开”永远显示；窗口隐藏后不在任务栏；退出只由退出菜单/系统动作触发 | Windows 常驻 | 单击两次状态可预测；隐藏时任务栏无按钮；重启后托盘状态正确 | 双击可能与两次 click 竞争；需按 Windows 实际事件序列验证；已有代码，未运行验证 |
| E02 | R/K | 托盘真实状态菜单 | 菜单显示引擎、更新、剪贴板哨兵、后台队列和失败原因；状态改变后重建菜单 | 常驻后台 | 每个可点菜单项都有动作；禁用项不会冒充可用；状态与界面一致 | 状态更新滞后会误导用户；菜单必须是最新快照；已有代码，未运行验证 |
| E03 | R/K | 全局快捷键协商 | 首选键失败时尝试备选；返回实际生效键；两者都失败时显示原因，不默认为“已启用” | 高频检索、截图直搜 | `register` false、冲突、非法组合和实际生效键都有不同结果 | Electron 对占用键会静默返回 false；当前代码已有处理，需端到端按键验证 |
| E04 | R/K | 命令面板统一入口 | 搜索、投喂、最近内容、设置、更新、诊断都可搜索；按拼音/关键词匹配 | 多功能桌面端 | 任何一级功能最多 2 次键盘动作可达；结果有真实 callback | 命令存在但无 callback 是装饰按钮；必须逐项触发用户可见结果；局部能力未闭环 |
| E05 | R/K | 字体与设计令牌守门 | 普通 UI 14pt/约18.67px，图表/表格/SVG/Canvas 16pt/约21.33px；中文 SimSun，西文 Times New Roman，图标 1em | 全部界面 | 令牌审计 0 条硬编码违规；1366×768 与 1920×1080 无关键溢出 | 全局放大可能使图表拥挤；按层级调节，不能一刀切；已有脚本，未完成浏览器复核 |
| E06 | G/N | 页面标题去重 | 已由导航表达的功能不再在左上角重复显示；保留当前上下文、项目和状态 | 所有页面 | 页面导航、浏览器/窗口标题和主舞台层级清晰；没有重复大标题 | 去掉后可能丢上下文；用面包屑/图标 tooltip 补足，不重新占主舞台；候选新增 |
| E07 | R/K | 空状态与失败状态设计 | 空结果、引擎未就绪、依赖缺失、网络失败、无权限、未验证分别呈现下一步动作 | 全部异步功能 | 每种状态至少有状态、原因、可操作恢复和原始错误入口 | catch 后只返回空数组会形成静默空白；需构造真实失败场景；局部能力未闭环 |
| E08 | R/K | 虚拟列表与稳定滚动 | 结果只渲染可视区，分阶段追加保持滚动锚点；不在排序时把焦点/选择清掉 | 大库、长简报 | 10000 条结果滚动 ≥55fps；追加结果不跳屏；键盘焦点保持 | 过度虚拟化会丢失可复制文本；提供当前窗口上下文和导出入口；已有能力，未测 |
| E09 | N | 操作成本雷达 | 对每个主要流程显示步骤数、可撤回性、等待阶段和下一步；用于发现“按钮很多但不工作” | 新手与高频用户 | 搜索、拖入、回查、导出、配对各有可复现路径；关键流程 ≤3 个主动作 | 只追求少步骤会隐藏必要确认；把安全/隐私确认保留在关键节点；候选新增 |
| E10 | G/N | 三种密度与沉浸图 | 标准/紧凑/宽松列表，核心图支持全屏、缩放、平移和回查抽屉 | 长时阅读、大屏 | 三种密度不改变事实和顺序；全屏图可返回原节点与动画时间 | 缩放过度使文字不可读；图内文字最低 16pt；候选新增 |

### F. 安卓、局域网与更新

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| F01 | R/K | 配对会话与权限收紧 | QR/口令只传最小地址和短期令牌；服务端校验来源、过期时间、重放和权限 | 安卓↔Windows 局域网 | 过期令牌、重放、错误设备和非局域网地址均被拒；失败原因可见 | 当前是局域网明文 HTTP + token 边界，不能当作端到端加密；局部能力未闭环 |
| F02 | N | 局域网 HTTPS/设备证书 | 初次 QR 带设备公钥指纹，后续用 HTTPS 或等效双向认证；证书变更必须重新配对 | 有敏感库、公共 Wi-Fi | 中间人替换证书时连接失败并提示重新配对；正常 LAN 延迟增加在预算内 | 自签证书轮换和恢复复杂；需清晰的重新配对路径；候选新增 |
| F03 | R/K | 安卓离线缓存分层 | 缓存最近结果、证据摘要和缩略图，显示获取时间与来源；写入操作进入 outbox | 断网、移动场景 | 断网仍能打开最近 N 条；恢复网络后 outbox 不重复提交 | 缓存可能过时；任何旧结果必须有时间标签；已有最近缓存，离线写入未闭环 |
| F04 | N | 冲突合并策略 | 以稳定 item ID、内容 hash 和版本时间合并；冲突保留双方，不默默覆盖 | 多设备投喂/同步 | 同一文件不同版本均可追踪；冲突可人工选择；重试幂等 | 时钟不可信时不能只比本地时间；使用服务器接收序列和内容 hash；候选新增 |
| F05 | R/K | 安卓更新三重检查 | 先比 `versionCode`，再校验 GitHub 资产大小/摘要，最后交系统安装器；包签名必须与已安装版本一致 | APK Release | 低版本能发现新版本；资产不匹配拒绝安装；版本号不增不发布 | Android 更新要求同签名；签名丢失会阻断所有用户更新；当前代码已有部分逻辑，未真机闭环 |
| F06 | R/K | 桌面更新链路锁定 | 构建产物、`latest.yml`、版本号、Release channel、sha512 和安装器必须同一批生成 | Windows NSIS | 新安装能检查/下载/安装；缺 latest.yml、草稿 Release、版本漂移和摘要不一致都可见失败 | 无 Authenticode 时仍存在 SmartScreen/发布者风险；不能宣称“安全更新”；已有代码，需安装版验证 |
| F07 | N | 双通道发布 manifest | 一个候选清单记录桌面 exe、latest.yml、APK、签名证书指纹、sha256、版本、隐私扫描和回滚 Release | 正式发布 | 上传前清单与远端资产逐项一致；缺项阻止发布 | 清单本身含路径/哈希但不应含密钥；发布前临时生成并清理；候选新增 |
| F08 | N | 版本/通道可回滚 | stable/beta 分离；客户端记录当前版本、目标版本、失败原因和恢复到上一版的路径 | 频繁发布 | 错误版本不会覆盖 stable；升级失败后仍可启动并显示恢复说明 | 回滚包必须兼容数据迁移；数据库迁移需前向/回滚策略；候选新增 |

### G. 安全、隐私、供应链与交付

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| G01 | R/K | 密钥不出仓库、不进 argv | safeStorage/系统凭据管理器保存 API key 和库口令；子进程通过受控环境或 IPC 取密钥 | 云搜索、加密库、配对 | 高置信密钥扫描 0；进程命令行不出现库口令/配对令牌/API key | 当前兼容常量仍允许部分 secret 进 argv；若直接删除会让配对静默失效，需先补引擎环境变量读取 |
| G02 | R/K | 数据出站双开关 | 网络搜索（查询词）与云端推理（内容）分别控制；每次出站记录目的地、字段和原因 | 隐私优先、混合模型 | 关闭任一开关后对应请求 0 次；MCP/CLI 不能绕过 | 只藏 UI 按钮不够；必须在引擎路由层阻断并用抓包/日志复核；已有设计，运行态未验证 |
| G03 | R/K | 出站白名单与审计 | 域名、协议、HTTP 方法和字段级脱敏白名单；原始请求摘要写本地审计日志 | 联网搜索、模型 API、更新 | 非白名单主机 0 次；日志不保存完整敏感内容；每次可解释 | 过宽白名单等于无控制；重定向和 CDN 主机也需显式解析；局部能力未闭环 |
| G04 | R/K | Electron 安全边界 | 远程内容关闭 Node 集成、开启 context isolation/sandbox、限制导航/新窗口/权限请求、验证 IPC sender | 网页抓取、PDF、远程来源 | 安全配置审计无高危项；不可信页面不能调用文件/密钥/设置 IPC | Electron 官方明确要求 sender 校验与严格 CSP；已有部分实现，需逐窗口复核 |
| G05 | N | ASAR/产物完整性 | 构建时生成完整性清单，安装后探针验证关键资源；异常进入修复/重新安装路径 | 桌面安装包 | 关键资源缺失或哈希不符时不继续执行敏感功能；诊断可见 | 完整性不是代码签名，不能阻止拥有本机权限的篡改；需准确描述保护边界；候选新增 |
| G06 | N | 依赖与 SBOM | 锁文件、运行时打包依赖和原生二进制分别生成 SBOM；CVE 扫描与许可证审计 | Node/Python/Android | 依赖来源、版本、许可证和漏洞状态可导出；高危项阻止发布 | CVE 数据会随时间变化；报告需写扫描日期和数据库版本；候选新增 |
| G07 | R/K | 隐私发布闸门 | 发布前排除本地库、模型、日志、绝对路径、工作流原话、私钥和备份；高置信命中阻止，技术词误报单独记录 | GitHub 公开仓库与 Release | Gitleaks/高置信扫描无泄露；候选资产仅列允许文件 | 通用 gate 可能误报 `tokens.txt` 等技术词；必须保留 BLOCK/误报事实，不篡改成 PASS |
| G08 | R/K | 最小权限与可恢复删除 | 回收站优先；删除索引与删除原文件分开；Release 只在锁定“最近两版”且核对 tag 后删除 | 清理、发版、用户数据 | 未授权删除 0；删除范围和恢复路径有原始记录 | “删旧版本”是不可逆动作；执行前必须重新确认清单、远端和本地目标 |
| G09 | N | 安全事故恢复包 | 输出可脱敏的诊断包：版本、环境能力、失败阶段、非敏感摘要和恢复建议 | 用户支持、安全响应 | 诊断包不含原文、密钥、绝对私密路径；可复现关键环境 | 过度脱敏会丢失根因；字段级策略需人工抽查；候选新增 |
| G10 | G/N | 独立安全复核 | 规划者、执行者、验收者分离；代码、依赖、产物、更新和隐私结果分别签名/记录 | 正式发布、重大改动 | 至少一份独立复核记录；执行者不能自证所有结论 | 没有第二上下文时只能标未完成；不能用 Agent 报告替代复核 |

### H. 用户端验证与持续质量

| ID | 类型 | 方案 | 作用与机制 | 适用范围 | 验收指标 | 失败表现、限制与状态 |
|---|---|---|---|---|---|---|
| H01 | P0/R | 用户端复现记录 | 从真实入口启动、操作、观察界面/文件/响应和日志；记录入口、步骤、期望、实际和原样证据 | 所有 bug 修复 | 修复前后跑同一场景；没有复现证据不得改 bug 根因 | 编译成功或单测通过不能替代；本轮明确不新增单元测试 |
| H02 | P0/R | 自检与实际损坏双轨 | 自检检查遗漏/空实现/无效配置；损坏检测检查真实启动、点击、数据流、状态和用户结果 | 全项目审查 | 两轨有分开的报告和结论；任一失败不会被另一轨覆盖 | “代码存在”会制造假绿；需把静默失败单独列出 |
| H03 | R/K | 浏览器真实验证 | 真实打开主页面和 HTML；检查 JS 错误、字体、尺寸、动画控制、筛选、回查和状态恢复 | GUI、HTML、证据图 | 1366×768、1920×1080 或相近视口无关键遮挡；控制器可操作 | 只看源码或 session wrapper 不够；iframe/artifact 也必须检查 |
| H04 | N | 故障注入清单 | 断网、引擎未起、依赖缺失、磁盘不足、权限拒绝、任务中断、更新资产缺失逐条观察 | 稳定性与安全 | 每个故障有可见状态、原始原因、恢复路径和不变数据证明 | 不能在没有恢复点时强杀真实库；使用临时测试库和可回收目录 |
| H05 | N | 跨端黄金路径 | Windows：托盘/快捷键/拖入/搜索/导出/更新；安卓：配对/搜索/拍照/分享/离线/更新；MCP：发现/搜索/证据 | 交付前 | 每条路径都有设备、版本、输入、期望、实际和原始证据 | 无真实 Android/安装包时标“未验证”，不能用编译替代 |
| H06 | N | 变更后回归边界 | 只回归改动影响的功能链，但包含一个保留的有效路径和一个故障路径 | 低风险增量修改 | 改动符号、上游输入和下游输出均有证据；不扩散无关改动 | 只跑局部类型检查可能漏运行时；需要按风险增加端到端路径 |
| H07 | G/N | 可审计交付报告 | 输出完成状态、证据类型、原始命令、版本/哈希、未验证项、残余风险和回滚动作 | 方案落地、Release | 每一项都能回到文件/日志/截图/远端资产；未验证不写完成 | 旧文档与当前事实冲突时保留冲突并标时间、版本、来源 |
| H08 | G/N | 用户选择回流 | HTML 选择板生成明确选择结果、版本化方案快照和执行范围；不把未选项目偷偷排进实施 | 方案阶段 | 选择项可导出；执行阶段只读取冻结快照；可撤销未执行选择 | 页面选择和账本不一致会导致范围漂移；以冻结快照为单一执行输入 |

## 推荐首批选择

如果目标是先让 Synorive 真实可靠地工作，建议首批冻结：

`P-A + G-A + A01 A02 A03 A08 A09 + B01 B02 B04 B08 B10 + C01 C02 C03 C04 C05 + D01 D02 D03 + E01 E02 E03 E05 E06 E07 E08 + F01 F03 F05 F06 F07 + G01 G02 G03 G04 G07 G08 + H01 H02 H03 H04 H05 H07 H08`

这组的理由是：它先把现有能力变成可证明的闭环，再加功能。A05、B03、B05、B06、C06、C07、D04–D06、E09–E10、F02、F04、F08、G05–G06、G09 和 H06 属于第二批候选；不是因为实现成本，而是它们依赖第一批稳定的数据/状态/证据边界。

## 现有基线中最需要反方审查的三件事

1. **“已有代码”不等于功能有效。** 现有桌面端已经有托盘、快捷键、更新、性能面板和多种结果组件，但工作区仍存在大量未提交改动，且项目自己的简报把很多基准标成未测。失败机理是事件/IPC/渲染/引擎任一层字段不一致时，界面可能得到空数组或旧状态而不报错。修正是 H01–H03，验证是从真实安装版或真实开发入口跑同一条用户路径。
2. **“极速”可能以质量换来的缩水。** 研究 deadline 可以降低 P95，却可能跳过第二轮检索；只看延迟会把降级结果误当成优化。修正是 C01，把来源数、第二轮完成率、降级率和 P95 放在同一验收记录中。
3. **“安全更新”目前有明确上限。** Electron 更新需要发布元数据和校验；安卓更新要求同签名；桌面端若没有代码签名，仍有 SmartScreen 和发布者信任风险。修正是 F05–F07、G01、G07，最终只能按实际签名、资产和安装复验结果表述。

## 当前阶段验收

- 已完成：当前方案库文件生成；已按本地代码/文档基线区分“已有、局部、候选”；已生成三个 GUI 方向预览和一个交互选择板。
- 部分验证：方案引用的当前项目事实来自工作区源码、README、SPEC、TECH_ROADMAP、`engine/synorive/metrics.py` 和安卓/桌面配置；尚未把这些候选方案实施到应用。
- 未验证：真实安装版托盘/快捷键/更新、真实 Android 设备、完整多模态黄金集、断电恢复、完整出站抓包、GitHub 远端发布状态和用户对 GUI 的最终选择。

## 原始资料与官方边界

- Electron Tray API：<https://www.electronjs.org/docs/latest/api/tray/>
- Electron 全局快捷键：<https://www.electronjs.org/docs/latest/api/global-shortcut/>
- Electron 安全清单：<https://www.electronjs.org/docs/latest/tutorial/security>
- electron-builder 自动更新：<https://www.electron.build/docs/features/auto-update/>
- electron-builder 发布配置：<https://www.electron.build/publish/>
- Android 应用签名与更新：<https://developer.android.com/studio/publish/app-signing>
- Android App Bundle / versionCode：<https://developer.android.com/guide/app-bundle/configure-base>
- SQLite FTS5：<https://www.sqlite.org/fts5.html>
- Model Context Protocol specification：<https://modelcontextprotocol.io/specification/2025-06-18/server/index>

这些链接只证明相应平台的机制和约束；它们不证明 Synorive 当前实现已经通过运行验证。当前实现和状态必须以本工作区文件与后续用户端证据为准。
